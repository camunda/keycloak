---
name: Renovate Auto Merge

on:
    pull_request:
        types: [opened, reopened, ready_for_review]

jobs:
    lint:
        # TODO: pin the version
        uses: camunda/infraex-common-config/.github/workflows/automerge-global.yml@feature/renovate-automerge-ci
        secrets: inherit
