---
name: lint

on:
    workflow_dispatch:
    pull_request:
    push:
        branches: [main]

jobs:
    lint:
        uses: camunda/infraex-common-config/.github/workflows/lint-global.yml@cb673a2ce24ba79afed0507f3d245cdecf17337f # 1.4.8
        secrets: inherit
