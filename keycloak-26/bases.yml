---
sources:
    hub:
        # List of all available images with associated sha:
        # skopeo --override-os linux inspect  docker://docker.io/bitnamilegacy/keycloak
        # Note: use the global index image digest to make this image platform agnostic
        # skopeo --override-os linux inspect docker://docker.io/bitnamilegacy/keycloak:<tag> --raw | jq '.Digest'
        image:
            repository: docker.io/bitnamilegacy/keycloak
            tag: 26.3.3-debian-12-r0@sha256:da3df0976a9f9a664bdbde6cb5308b78f03ac94d0abf33b2df355bbb06cbc5b9

    prem:
        # List of all available images with associated sha:
        # skopeo --override-os linux inspect docker://registry.camunda.cloud/vendor-ee/keycloak
        # Note: use the global index image digest to make this image platform agnostic
        # skopeo --override-os linux inspect docker://registry.camunda.cloud/vendor-ee/keycloak:<tag> --raw | jq '.Digest'
        image:
            repository: registry.camunda.cloud/vendor-ee/keycloak
            tag: 26.5.0-debian-12-r3@sha256:554a3fa354ffef549dabda0647dce666c282a5712a42d490f756f4d39edcb7f8

    quay:
        # List of all available images with associated sha:
        # skopeo --override-os linux inspect docker://quay.io/keycloak/keycloak
        # Note: use the global index image digest to make this image platform agnostic
        # skopeo --override-os linux inspect docker://quay.io/keycloak/keycloak:<tag> --raw | jq '.Digest'
        image:
            repository: quay.io/keycloak/keycloak
            tag: 26.5.5@sha256:a7b0cb7a43a1235a61872883414d3f1d9a3ceac9df6e5907bd12202778a6265c
